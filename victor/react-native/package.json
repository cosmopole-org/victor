{
  "name": "victor-react-native",
  "version": "0.1.0",
  "description": "React Native / Expo host for the Victor engine: the Elpian VM drives a cross-platform 2D React Native widget tree with embedded 3D Godot Scene3D worlds.",
  "license": "BSD-3-Clause",
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "typecheck": "tsc --noEmit",
    "test:host": "node --experimental-strip-types test/host.test.ts && node --experimental-strip-types test/style.test.ts",
    "build:wasm": "cd ../ && cargo build -p elpian-rn --target wasm32-unknown-unknown --release && cp target/wasm32-unknown-unknown/release/elpian_rn.wasm react-native/web/elpian_rn.wasm"
  },
  "dependencies": {
    "expo": "~52.0.0",
    "expo-status-bar": "~2.0.0",
    "react": "18.3.1",
    "react-native": "0.76.3"
  },
  "devDependencies": {
    "@types/react": "~18.3.12",
    "typescript": "~5.6.3"
  },
  "private": true
}
