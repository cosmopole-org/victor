// GENERATED FILE — do not edit by hand.
//
// This is the committed *stub*. Run `dart run tool/gen_registry.dart` (with a
// Flutter SDK available) to overwrite it with the full public-widget registry
// generated from Flutter's own API — the mechanism that makes host-side widget
// coverage complete (see ../FLUTTER.md, "How 'no exception' is achieved"). Until
// then this returns null and the hand-written catalog in main.dart is the
// coverage; the guest side (`FL.el` + any handler in any position) is already
// complete by construction and tested.

part of 'main.dart';

/// Fallback builder for any widget type not in main.dart's hand-written switch.
/// The generator replaces this with a large `switch (type)` that constructs
/// every public Flutter widget by mapping its constructor parameters from the
/// props (`p`) through the type-directed decoders on `_P`.
Widget? buildGenerated(String type, _P p, List raw, Key? key, _Interp it) => null;
