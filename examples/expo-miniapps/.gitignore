# Dependencies & Expo caches
node_modules/
.expo/
.expo-shared/

# Build output (regenerated by `npm run export:web`)
dist/

# The locally-packed library tarball (built by `npm run pack:lib`)
*.tgz

# The VM module synced from the installed package (built by `npm run sync:wasm`)
public/elpian_rn.wasm

# Playwright end-to-end screenshot
e2e-screenshot.png

*.log
